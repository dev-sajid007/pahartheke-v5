services:
  mongo:
    image: mongo:7
    container_name: pahar-pos-mongo
    restart: unless-stopped
    ports:
      - "27018:27017"
    volumes:
      - mongo-pos-data:/data/db

volumes:
  mongo-pos-data: